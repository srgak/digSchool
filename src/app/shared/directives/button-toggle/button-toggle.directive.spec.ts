import { ButtonToggleDirective } from './button-toggle.directive';

describe('ButtonToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonToggleDirective();
    expect(directive).toBeTruthy();
  });
});
