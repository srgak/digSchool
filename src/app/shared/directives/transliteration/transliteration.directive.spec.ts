import { TransliterationDirective } from './transliteration.directive';

describe('TransliterationDirective', () => {
  it('should create an instance', () => {
    const directive = new TransliterationDirective();
    expect(directive).toBeTruthy();
  });
});
