import { SurnameNPPipe } from './surname-np.pipe';

describe('SurnameNPPipe', () => {
  it('create an instance', () => {
    const pipe = new SurnameNPPipe();

    expect(pipe).toBeTruthy();
  });
});
